fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
python-dotenv>=1.0

# Optional: only needed if you set ANTHROPIC_API_KEY to get AI-written pick
# explanations. The app runs fully without it (deterministic templates).
anthropic>=0.40
